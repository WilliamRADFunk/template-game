export const MAX_COLS = 29;

export const MAX_ROWS = 29;

export const MIN_COLS = 0;

export const MIN_ROWS = 0;

export const MIDDLE_COL = Math.ceil((MAX_COLS - MIN_COLS) / 2);

export const MIDDLE_ROW = Math.ceil((MAX_ROWS - MIN_ROWS) / 2);